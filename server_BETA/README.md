# Rapido Receipt Extraction API

This is a lightweight Flask API designed to parse directories of Rapido PDF receipts and return the extracted financial and location data in a structured JSON format.

## Installation

1. Ensure Python is installed on your system.
2. Install the required dependencies:
   ```bash
   pip install -r requirements.txt